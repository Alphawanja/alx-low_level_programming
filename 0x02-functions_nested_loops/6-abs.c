#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @a: the integer to be computed
 * Return: the absolute value
 */
int _abs(int a)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
